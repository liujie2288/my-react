// 渲染React元素
export default function renderComponentElement(virtualDom, container) {
  // React元素有两种形式： function组件 、  class组件
  // function组件直接调用，返回值为新的virtualDom
  // class组件需要先实例化，在调用实例的render方法

}
